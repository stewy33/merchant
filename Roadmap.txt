PRIORITY:
1. Command line tool
   a. Learn how to install a mercury library and use json parser.
   b. Learn how to use mercury command line parser.
   c. Define syntax for manifest file.
   d. Use the parser on the manifest file.
   e. Define directory structure for files.
   f. Using manifest file, clone a git repo into a desired folder.
   g. Write build commands. (specifies to use which libraries from where based on the manifest file).
   h. Build the dependency tree.
   i. Make it not download the same package twice.
   j. Learn to download package from a registry using this tool.


2. Make the registry...

Completed: 
1: a,c,d
